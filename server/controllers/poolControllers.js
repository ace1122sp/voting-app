const express = require('express');
const mongoose = require('mongoose');

module.exports = {
  getPools: () => {},
  createPool: () => {},
  getPool: () => {},
  deletePool: () => {},
  vote: () => {},
  followPool: () => {},
  unfollowPool: () => {},
  addOption: () => {},
  removeOption: () => {}
}